const { ArrayBen: Array } = require('../../../../__data_structures/1_arrays');

const main = () => {
	let a = new Array([1, 3, 5, 7]);
	// a.merge([2, 4, 6,  8])
	// a.union([2, 4, 6,  8])
	// a.intersection([2, 4, 1231])
	// a.difference([1, 4, 1231])

}
 
main();